package app.model;

import app.model.generic.Person;

public class EnviromentalManager extends Person {
    @Override
    public String toString() {
        return "EnviromentalManager{} " + super.toString();
    }
}
